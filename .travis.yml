language: java
jdk: openjdk8
addons:
    sonarcloud:
        organization: "fourcats" # the key of the org
        token: ${SONAR_TOKEN_NATURALAPI} # encrypted value of your token
script:
    # other script steps might be done before running the actual analysis
    - cd NaturalAPI_Discover
    - mvn clean package sonar:sonar -Psonar,coverage -Dsonar.projectKey=fourcatsteam_NaturalAPI
    #- mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.projectKey=fourcatsteam_NaturalAPI
    #- mvn clean verify sonar:sonar -Pcoverage -Dsonar.projectKey=fourcatsteam_NaturalAPI
    #- sonar-scanner

    - cd NaturalAPI_Design
    - mvn clean package sonar:sonar -Psonar,coverage -Dsonar.projectKey=fourcatsteam_NaturalAPI
    - sonar-scanner